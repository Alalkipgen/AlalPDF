name: Apply recovered PDF fixes
on:
  push:
    branches: [fix/pdf-workflow-and-editor]
permissions:
  contents: read
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Recovery already applied successfully.'
